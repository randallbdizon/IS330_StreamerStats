Idea for webpage:

- Login splash page to create user or to log in.
- If logged in, determine if user is admin
  - If user is admin, allow update options in Project page.
    - Determine that through back-end in DB.
  - If user is not admin, only allow view Project page.
